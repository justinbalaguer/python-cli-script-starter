starter for creating python cli scripts

install in develop mode:
`$ pip3 install -e jstn` <!-- jstn is the folder name / the roots folder name (make sure to cd ../ outside the project folder) -->

uninstall:
`$ pip3 uninstall jstn`